@model IEnumerable<Examen.ApplicationCore.Domaine.Invitation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateInvitation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmeInvitation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InviteFk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FeteFk)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateInvitation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConfirmeInvitation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InviteFk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FeteFk)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
